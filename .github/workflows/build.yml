name: Build (Isabelle)

# Verifies the BMSSP_Correctness session compiles with no proof holes.
# Runs the proofs (document disabled, so no LaTeX is needed in CI).

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: isabelle build BMSSP_Correctness
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      ISABELLE_VERSION: Isabelle2025-2
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Isabelle distribution
        id: cache-dist
        uses: actions/cache@v4
        with:
          path: ~/${{ env.ISABELLE_VERSION }}
          key: ${{ env.ISABELLE_VERSION }}-linux

      - name: Download Isabelle
        if: steps.cache-dist.outputs.cache-hit != 'true'
        run: |
          cd "$HOME"
          curl -fSL "https://isabelle.in.tum.de/dist/${ISABELLE_VERSION}_linux.tar.gz" -o isabelle.tar.gz
          tar -xzf isabelle.tar.gz
          rm isabelle.tar.gz

      - name: Put Isabelle on PATH
        run: echo "$HOME/${ISABELLE_VERSION}/bin" >> "$GITHUB_PATH"

      - name: Show Isabelle version
        run: isabelle version

      - name: Cache Isabelle heaps (HOL-Library, etc.)
        uses: actions/cache@v4
        with:
          path: ~/.isabelle
          key: isabelle-heaps-${{ env.ISABELLE_VERSION }}-${{ hashFiles('**/*.thy', 'ROOT') }}
          restore-keys: |
            isabelle-heaps-${{ env.ISABELLE_VERSION }}-

      - name: Build session (proofs; PDF disabled for CI)
        run: |
          # The committed ROOT requests a PDF (document = pdf). Building it needs a
          # LaTeX/lualatex toolchain; CI verifies the *proofs* (the kernel checks),
          # not the typesetting. A session-level ROOT option overrides the
          # command-line `-o document=false`, so we neutralise it in the ephemeral
          # runner checkout only. Local users still build the PDF with
          # `isabelle build -D .` against the unmodified ROOT.
          sed -i 's/document = pdf/document = false/' ROOT
          isabelle build -D .
